node_modules/
dist/
build/
.next/
.turbo/

*.log
npm-debug.log*
pnpm-debug.log*

.env
.env.local
.env.*.local
!.env.example

.DS_Store
Thumbs.db

.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

coverage/
.nyc_output/
*.tsbuildinfo

# Personal data — never commit Lucky's blueprint
docs/personal/

# Raw HKO scrape — 5.8 MB. Parsed JSON (data/solar-terms.json) is the artifact.
packages/bazi/data/hko-raw/

# Local-only CLI state: birth profile + diary log. Never commit personal data.
.semar/

# Server-side SQLite diary store (apps/web /api/diary). Holds personal diary
# entries — never commit. WAL/SHM sidecar files included.
# Anchored to repo root so it does NOT swallow packages/*/data (e.g. the
# vendored tarot corpora JSON, which is build-time runtime data).
/data/
